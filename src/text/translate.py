def translate_to_english(text: str) -> str:
    """Fallback translator.

    MVP keeps the original text. Replace this function with a real translation
    service or multilingual model when the team integrates CLIP.
    """
    return text

